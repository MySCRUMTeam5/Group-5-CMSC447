services:
  # The Backend playhouse
  backend:
    build: ./backend
    ports:
      - "8002:8000"
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=1

  # The Frontend playhouse
  frontend:
    build: ./hoardhero-frontend
    ports:
      - "5174:5173"
    volumes:
      - ./hoardhero-frontend:/app
      - /app/node_modules
    depends_on:
      - backend
